name: streak_counters
description: Duolingofy everything.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  objectbox: ^4.0.1
  objectbox_flutter_libs: ^4.0.1
  english_words: ^4.0.0
  provider: ^6.1.2
  clock: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11
  objectbox_generator: ^4.0.1
  flutter_launcher_icons: ^0.13.1

  flutter_lints: ^4.0.0
  test: ^1.25.2

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "./assets/app_icon.png"